import 'package:flutter/material.dart';

const mainColor = Color(0xff41348C);
final whiteColor = Colors.white;
final greyColor = Colors.grey;

final greyFontStyle =
    ThemeData().textTheme.bodyText1.copyWith(color: greyColor);
